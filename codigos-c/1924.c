#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n;
    char s[101];
    scanf("%d", &n);
    while(n--)
    {
        scanf("%s", s);
    }
    printf("Ciencia da Computacao\n");
    return 0;
}

