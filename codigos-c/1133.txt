#include <stdio.h>

int main()
{
    int x,y,m,p,i,aux;
    scanf("%d %d", &x, &y);
    if(x > y)
    {
        aux = x;
        x = y;
        y = aux;
    }
    for(i=x+1;i < y;i++)
    {
        if(i % 5 == 2 || i % 5 == 3)
            printf("%d\n", i);
    }
    return 0;
}
#include <stdio.h>

int main()
{
    int n;
    while(n != 2002)
    {
        scanf("%d", &n);
        if(n==2002)
            printf("Acesso Permitido\n");
        else
            printf("Senha Invalida\n");
    }
    return 0;
}

